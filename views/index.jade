extends layout

block content
    h1= title
    p Welcome to #{title}
    each channel in channels
        div(id="#{channel.id}")
            span #{channel.name} -
            span #{channel.description}